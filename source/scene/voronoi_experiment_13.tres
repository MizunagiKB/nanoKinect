[gd_resource type="CanvasItemShader" format=1]

[resource]

_code = {
"fragment": "vec4 col = tex(TEXTURE, vec2(UV.x + sin(SCREEN_UV.y*8 + TIME) / 16, UV.y + TIME  * 0.4));
//vec3 col = texscreen(vec2(SCREEN_UV.x, SCREEN_UV.y));
//vec3 col = tex(SCREEN_UV);

COLOR.r = cos(col.r * TIME);
COLOR.g = cos(col.g * TIME);
COLOR.b = cos(col.b * TIME);
COLOR.a = 0.1;
//COLOR.a = 0.1;
",
"fragment_ofs": 0,
"light": "",
"light_ofs": 0,
"vertex": "",
"vertex_ofs": 0
}

